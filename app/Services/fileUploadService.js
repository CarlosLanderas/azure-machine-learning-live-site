import $ from "jquery";

const fileUploadService = ({ files, path }) => {
    
    let uploadPromise = new uploadPromise( (reject, resolve) =>
    {
        //Pending 
        
    });

    return uploadPromise;

};

export default fileUploadService;